import os
import shutil

src = os.listdir(os.getcwd())

if src not empty:
		# Create a folder
		for files in src:
				# If files have an extension that end in .exe 
				if files.endswith(".exe")
						try:
								os.makedirs('applications');
						except:
								Print("Folder already exists")
		 					shutil.move(files, dst)
		




dst = os.getcwd() + "\\applications"
dst = os.getcwd() + "\\applications"
dst = os.getcwd() + "\\applications"
dst = os.getcwd() + "\\applications"